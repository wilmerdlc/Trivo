# ==========================================
# Local development only (compose.yaml)
# For the full stack, incl. the dockerized API (compose.prod.yaml), use
# .env.template instead — it includes these same variables plus the rest.
# ==========================================

# PostgreSQL
POSTGRES_USER=xxx
POSTGRES_PASSWORD=xxx
POSTGRES_DB=xxx
POSTGRES_PORT=xxx

# PgAdmin
PGADMIN_DEFAULT_EMAIL=xxx
PGADMIN_DEFAULT_PASSWORD=xxx
PGADMIN_PORT=xxx

# Seq (Logging)
SEQ_ACCEPT_EULA=xxx
SEQ_ADMIN_PASSWORD=xxx
SEQ_PORT_INGESTION=xxx
SEQ_PORT_UI=xxx

# Redis (Cache)
REDIS_PASSWORD=xxx
REDIS_PORT=xxx
