# ==========================================
# Local (compose.yaml)
# ==========================================

# PostgreSQL
POSTGRES_USER=xxx
POSTGRES_PASSWORD=xxx
POSTGRES_DB=xxx
POSTGRES_PORT=xxx

# PgAdmin
PGADMIN_DEFAULT_EMAIL=xxx
PGADMIN_DEFAULT_PASSWORD=xxx
PGADMIN_PORT=xxx

# Seq (Logging)
SEQ_ACCEPT_EULA=xxx
SEQ_ADMIN_PASSWORD=xxx
SEQ_PORT_INGESTION=xxx
SEQ_PORT_UI=xxx

# Redis (Cache)
REDIS_PASSWORD=xxx
REDIS_PORT=xxx

# ==========================================
# Production (compose.prod.yaml)
# In addition to everything above (same Postgres/Redis/Seq/PgAdmin).
# ==========================================

# API image and published port
API_IMAGE=wdelacruzdev/trivo-backend:xxx
API_PORT=xxx

# JWT
JWT_KEY=xxx
JWT_ISSUER=xxx
JWT_AUDIENCE=xxx
JWT_DURATION_IN_MINUTES=xxx

# Email (SMTP)
EMAIL_FROM=xxx
EMAIL_SMTP_HOST=xxx
EMAIL_SMTP_PORT=xxx
EMAIL_SMTP_USER=xxx
EMAIL_SMTP_PASSWORD=xxx
EMAIL_DISPLAY_NAME=xxx

# Cloudinary
CLOUDINARY_URL=xxx

# AI (OpenAI)
AI_EMBEDDING_MODEL=text-embedding-3-small
AI_API_KEY=xxx

# CORS — the real frontend origin allowed to call the API in production
CORS_ALLOWED_ORIGIN=xxx
